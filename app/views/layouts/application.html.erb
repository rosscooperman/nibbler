<!DOCTYPE html>
<html lang="en">
<head>
  <title><% unless @page_title.blank? %><%= @page_title %> &mdash; <% end %><%= company_name %></title>
  <%= csrf_meta_tags %>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  <meta name="description" content=""/>
  <meta name="keywords" content="<%= company_name %>" />
  <meta name="author" content="<%= company_name %> www.<%= company_name %>.me" />
  <link rel="shortcut icon" type="image/x-icon" href="favicon.ico" />

  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>

  <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->

  <%= yield :head -%>
</head>

<% aside = yield(:aside) %>

<%= body_tag %>
  <%= content_tag(:div, "Staging Server", :id => "staging") if staging? %>

<div class="fullMap">
	MAP script goes here
</div>

<div class="container wrapper">
<header class="container">
	<section class="row">
		<div class="five columns">
			<h2>
				<%= link_to "Nibble.me", root_path, :rel => "#{company_name} Home Page" %>
        <%#= render :partial => "layouts/nav" %>
      </h2>
		</div>
	</section>
</header>

<section class="container">
	<div class="row">
		<div class="seven columns centered searcherMain">
			<h2>Search for your favorite food&nbsp;truck, cuisine, or location.</h2>
			<ul class="block-grid two-up">
				<li>
					<input type="text" name="some_name" value="" placeholder="Start Searching" class="nice oversize input-text">
				</li>
				<li>
					<a href="#" class="nice medium radius blue button">Search</a>
				</li>
			</ul>
		</div>
	</div>
</section>

<footer class="container">
  <div class="row noBackground">
		<div class="twelve columns">
   		<%= render :partial => "layouts/footer" %>
		</div>
  </div>
</footer>

<div class="conatiner">
	<div class="row noBackground">
		<div class="four columns centered" style="text-align: center;">
			<p class="copyright">&copy;
			  <%= Time.now.year %> <%= link_to company_name, root_path, :title => "Return to the #{company_name} Home Page", :rel => "#{company_name} Home Page" %>
			   | All Rights Reserved.
			</p>
		</div>
	</div>
</div>

</div>
  <%= yield :bottom %>
  <%= git_version_html %>

	<!-- BRETT UPDATE -->
	<%= javascript_include_tag "foundation" %>
	<%= javascript_include_tag "app" %>

</body>
</html>